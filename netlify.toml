# Netlify ne sert ici qu'à héberger la fonction OAuth de l'éditeur (admin.html).
# Le site reste publié par GitHub Pages ; seule /.netlify/functions/auth est utilisée.
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
